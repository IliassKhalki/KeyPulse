# KeyPulse Downloads

Download `KeyPulse-portable.zip`, extract it, then run:

```text
KeyPulse\KeyPulse.exe
```

Keep the extracted `KeyPulse` folder together. The executable needs the bundled `_internal` folder beside it.

For a cleaner public distribution later, publish this zip as a GitHub Release asset instead of storing binaries in the repository.
